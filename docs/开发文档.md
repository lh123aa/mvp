# 开发文档

## 概述

本文档详细介绍 Demand Miner 项目的技术架构、模块设计和开发规范。

---

## 技术架构

### iFlow CLI Skills 架构

Demand Miner 基于 iFlow CLI 的 Skills 框架构建，采用标准化的技能配置和执行模式。

```
┌─────────────────────────────────────────┐
│           iFlow CLI Engine              │
├─────────────────────────────────────────┤
│  skill.yaml (触发配置)                  │
│  SKILL.md (技能文档/SOP)                │
├─────────────────────────────────────────┤
│  skills/__init__.py (入口)              │
├─────────────────────────────────────────┤
│  scripts/ (业务逻辑)                    │
│  resources/ (数据资源)                  │
└─────────────────────────────────────────┘
```

---

## 模块设计

### 1. 技能入口 (skills/__init__.py)

**职责**：作为 Skills 包的入口点，加载和导出核心功能

**功能**：
- 读取 SKILL.md 文档内容
- 导出技能元数据（名称、版本、描述）
- 提供模块导入接口

```python
# 核心导出
__all__ = ['SKILL_CONTENT', 'get_skill_info']
```

### 2. 辅助脚本 (scripts/)

#### 2.1 constraint_filter.py

**职责**：约束过滤逻辑，根据用户预算和周期过滤方案

**函数**：
```python
def filter_by_budget(solutions: list, budget: str) -> list
def filter_by_timeline(solutions: list, timeline: str) -> list
def filter_solutions(solutions: list, constraints: dict) -> list
```

**约束规则**：
| 预算 | 过滤规则 |
|------|----------|
| 0成本 | 过滤所有付费方案 |
| 5000元内 | 过滤成本>5000的方案 |
| 5000-20000元 | 过滤成本>20000的方案 |

| 周期 | 过滤规则 |
|------|----------|
| 1个月 | 过滤周期>1个月的方案 |
| 3个月 | 过滤周期>3个月的方案 |
| 6个月 | 过滤周期>6个月的方案 |

#### 2.2 data_loader.py

**职责**：加载行业数据和问题配置

**函数**：
```python
def load_industries() -> dict
def load_questions() -> dict
def get_industry_data(industry: str) -> dict
```

---

## 数据资源

### 1. industries.json

**结构**：
```json
{
  "跨境电商": {
    "用户群体": [...],
    "轻量化方案": [...],
    "常见痛点": [...],
    "验证指标": {...}
  }
}
```

### 2. questions.json

**结构**：
```json
{
  "前置对齐": [
    {"id": 1, "问题": "...", "选项": [...]}
  ]
}
```

---

## SKILL.md 规范

SKILL.md 是技能的核心文档，遵循以下结构：

### 必需章节

1. **元数据**：版本、作者、触发关键词
2. **角色定义**：AI 角色和能力
3. **核心能力**：主要功能描述
4. **输出格式**：标准化输出模板
5. **响应规则**：交互约束

### 触发关键词配置

在 skill.yaml 中配置：
```yaml
triggers:
  - 产品分析
  - 项目分析
  - 商业分析
  - 商业闭环
  - 需求挖掘
  - 商业模式
  - 创业分析
  - 副业规划
```

---

## 开发规范

### 代码风格

- 使用 Python 3.x 语法
- 函数命名使用 snake_case
- 添加必要的类型注解
- 保持函数简洁单一职责

### 文件命名

- Python 模块：snake_case.py
- 配置文件：kebab-case.yaml / camelCase.json
- 文档：kebab-case.md

### Git 提交规范

```
feat: 新功能
fix: 修复
refactor: 重构
docs: 文档更新
chore: 其他更新
```

---

## 扩展开发

### 添加新行业

1. 在 `resources/industries.json` 中添加行业数据
2. 遵循现有结构：用户群体、方案、痛点、验证指标

### 添加新功能

1. 更新 `SKILL.md` 添加功能描述
2. 在 `scripts/` 中实现对应逻辑
3. 更新 `skill.yaml` 触发词（如需要）

---

## 测试

### 基础测试

```python
# 测试约束过滤
from scripts.constraint_filter import filter_by_budget

solutions = [
    {"name": "方案A", "cost": 1000},
    {"name": "方案B", "cost": 8000}
]
result = filter_by_budget(solutions, "5000元内")
assert len(result) == 1
```

```python
# 测试数据加载
from scripts.data_loader import load_industries

industries = load_industries()
assert "跨境电商" in industries
```

---

## 部署

### 本地测试

```bash
# 验证 JSON 格式
python -c "import json; json.load(open('resources/industries.json'))"

# 验证 Python 语法
python -m py_compile scripts/*.py
```

### 发布

1. 更新版本号（SKILL.md、skill.yaml）
2. 更新迭代日志
3. 提交并推送到 GitHub

---

## 常见问题

### Q: 如何修改触发关键词？
A: 编辑 `skill.yaml` 中的 `triggers` 列表

### Q: 如何添加新的分析模式？
A: 在 `SKILL.md` 中添加模式描述，并在 `skills/__init__.py` 中实现

### Q: 数据文件格式有什么要求？
A: 使用标准 JSON 格式，确保 UTF-8 编码

---

## 附录

- [SKILL.md](./SKILL.md) - 主技能文档
- [迭代日志](./docs/迭代日志.md) - 版本历史
- [运营计划](./docs/运营计划.md) - 项目记录
